'use strict';
// Получите от пользователя 2 числа

const findMinNumber = (numOne, numTwo) => {

  return (numOne + numTwo) / 2;


    // if (numOne < numTwo) {
    //   return numOne;
    // } else {
    //   return numTwo;
    // }
};

// let result = findMinNumber(+prompt('Введите первое число:'), +prompt('Введите второе число:'));

// console.log(`Минимальное число - ${result}`);

// Напишите функцию нахождения минимального числа, без использования условного оператора, циклов и массивов